# 类
## class FramebufferTexture
```cj
public open class FramebufferTexture <: Texture
```
Framebuffer 纹理类

### func init\(Int64,Int64,Int64\)
```cj
public init(width: Int64, height: Int64, format!: Int64 = RGBAFormat)
```
构造一个新的 Framebuffer 纹理

参数: 

|名称|类型|描述|
|---|---|---|
|width|Int64|纹理宽度（像素）height 纹理高度（像素）format 像素格式，默认 RGBAFormat|
|height|Int64||
|format|Int64||

