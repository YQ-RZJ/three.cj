# 函数
## func contain\(Texture,Float64\)
```cj
public func contain(texture: Texture, aspect: Float64): Texture
```
按包含模式缩放纹理（包级便捷函数，转发到 TextureUtils.contain）

参数: 

|名称|类型|描述|
|---|---|---|
|texture|Texture|目标纹理aspect 目标表面宽高比|
|aspect|Float64||

返回: 

- 被修改的纹理

## func cover\(Texture,Float64\)
```cj
public func cover(texture: Texture, aspect: Float64): Texture
```
按覆盖模式缩放纹理（包级便捷函数，转发到 TextureUtils.cover）

参数: 

|名称|类型|描述|
|---|---|---|
|texture|Texture|目标纹理aspect 目标表面宽高比|
|aspect|Float64||

返回: 

- 被修改的纹理

## func fill\(Texture\)
```cj
public func fill(texture: Texture): Texture
```
将纹理重置为默认变换（包级便捷函数，转发到 TextureUtils.fill）

参数: 

|名称|类型|描述|
|---|---|---|
|texture|Texture|目标纹理|

返回: 

- 被修改的纹理

## func getTextureTypeByteLength\(Int64\)
```cj
public func getTextureTypeByteLength(`type`: Int64): TypeByteLength
```
查表获取给定纹理类型对应的字节数与分量数

参数: 

|名称|类型|描述|
|---|---|---|
|`type`|Int64||

返回: 

- TypeByteLength 结构

