# 函数
## func handleIdx\(FrameBufferHandle\)
```cj
public func handleIdx(handle: FrameBufferHandle): UInt16
```
取帧缓冲句柄内部 idx（仅用于日志调试输出；有效性判断请用 isValidHandle）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|FrameBufferHandle||

## func handleIdx\(TextureHandle\)
```cj
public func handleIdx(handle: TextureHandle): UInt16
```
取纹理句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|TextureHandle||

## func handleIdx\(ProgramHandle\)
```cj
public func handleIdx(handle: ProgramHandle): UInt16
```
取程序句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|ProgramHandle||

## func handleIdx\(UniformHandle\)
```cj
public func handleIdx(handle: UniformHandle): UInt16
```
取 uniform 句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|UniformHandle||

## func handleIdx\(VertexBufferHandle\)
```cj
public func handleIdx(handle: VertexBufferHandle): UInt16
```
取静态顶点缓冲句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|VertexBufferHandle||

## func handleIdx\(IndexBufferHandle\)
```cj
public func handleIdx(handle: IndexBufferHandle): UInt16
```
取静态索引缓冲句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|IndexBufferHandle||

## func handleIdx\(VertexLayoutHandle\)
```cj
public func handleIdx(handle: VertexLayoutHandle): UInt16
```
取顶点布局句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|VertexLayoutHandle||

## func handleIdx\(DynamicVertexBufferHandle\)
```cj
public func handleIdx(handle: DynamicVertexBufferHandle): UInt16
```
取动态顶点缓冲句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|DynamicVertexBufferHandle||

## func handleIdx\(DynamicIndexBufferHandle\)
```cj
public func handleIdx(handle: DynamicIndexBufferHandle): UInt16
```
取动态索引缓冲句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|DynamicIndexBufferHandle||

## func handleIdx\(ShaderHandle\)
```cj
public func handleIdx(handle: ShaderHandle): UInt16
```
取着色器句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|ShaderHandle||

## func handleIdx\(IndirectBufferHandle\)
```cj
public func handleIdx(handle: IndirectBufferHandle): UInt16
```
取间接绘制缓冲句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|IndirectBufferHandle||

## func handleIdx\(OcclusionQueryHandle\)
```cj
public func handleIdx(handle: OcclusionQueryHandle): UInt16
```
取遮挡查询句柄 idx（日志用）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|OcclusionQueryHandle||

## func isValidHandle\(FrameBufferHandle\)
```cj
public func isValidHandle(handle: FrameBufferHandle): Bool
```
判断帧缓冲句柄是否有效（idx != 0xFFFF）

参数: 

|名称|类型|描述|
|---|---|---|
|handle|FrameBufferHandle||

## func isValidHandle\(TextureHandle\)
```cj
public func isValidHandle(handle: TextureHandle): Bool
```
判断纹理句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|TextureHandle||

## func isValidHandle\(ProgramHandle\)
```cj
public func isValidHandle(handle: ProgramHandle): Bool
```
判断程序句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|ProgramHandle||

## func isValidHandle\(UniformHandle\)
```cj
public func isValidHandle(handle: UniformHandle): Bool
```
判断 uniform 句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|UniformHandle||

## func isValidHandle\(VertexBufferHandle\)
```cj
public func isValidHandle(handle: VertexBufferHandle): Bool
```
判断静态顶点缓冲句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|VertexBufferHandle||

## func isValidHandle\(IndexBufferHandle\)
```cj
public func isValidHandle(handle: IndexBufferHandle): Bool
```
判断静态索引缓冲句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|IndexBufferHandle||

## func isValidHandle\(VertexLayoutHandle\)
```cj
public func isValidHandle(handle: VertexLayoutHandle): Bool
```
判断顶点布局句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|VertexLayoutHandle||

## func isValidHandle\(DynamicVertexBufferHandle\)
```cj
public func isValidHandle(handle: DynamicVertexBufferHandle): Bool
```
判断动态顶点缓冲句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|DynamicVertexBufferHandle||

## func isValidHandle\(DynamicIndexBufferHandle\)
```cj
public func isValidHandle(handle: DynamicIndexBufferHandle): Bool
```
判断动态索引缓冲句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|DynamicIndexBufferHandle||

## func isValidHandle\(ShaderHandle\)
```cj
public func isValidHandle(handle: ShaderHandle): Bool
```
判断着色器句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|ShaderHandle||

## func isValidHandle\(IndirectBufferHandle\)
```cj
public func isValidHandle(handle: IndirectBufferHandle): Bool
```
判断间接绘制缓冲句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|IndirectBufferHandle||

## func isValidHandle\(OcclusionQueryHandle\)
```cj
public func isValidHandle(handle: OcclusionQueryHandle): Bool
```
判断遮挡查询句柄是否有效

参数: 

|名称|类型|描述|
|---|---|---|
|handle|OcclusionQueryHandle||

