# 类
## class SkinWeights
```cj
public class SkinWeights
```
蒙皮权重输入

### func init\(Int,Array<Int16>,Array<Float32>\)
```cj
public init(numInfluences: Int, jointIndices: Array < Int16 >, jointWeights: Array < Float32 >)
```


参数: 

|名称|类型|描述|
|---|---|---|
|numInfluences|Int||
|jointIndices|Array<Int16>||
|jointWeights|Array<Float32>||

### let jointIndices
```cj
public let jointIndices: Array < Int16 >
```
关节索引数组

### let jointWeights
```cj
public let jointWeights: Array < Float32 >
```
权重数组

### let numInfluences
```cj
public let numInfluences: Int
```
影响数（1-4）

