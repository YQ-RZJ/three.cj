/**
 * @file[zh-cn] main three 引擎程序入口
 * @brief[zh-cn] three 引擎的可执行程序入口文件
 * @explain[zh-cn] 本文件为 three 引擎的程序入口，main 函数返回进程退出码。
 *
 * @file[en] main Entry point of the three engine program
 * @brief[en] Executable entry of the three engine
 * @explain[en] This file is the program entry of the three engine; the main function returns the process exit code.
 */
package three

/**
 * @brief[zh-cn] 引擎程序入口函数
 * @return[zh-cn] 进程退出码（0 表示正常退出）
 *
 * @brief[en] The engine program entry function
 * @return[en] The process exit code (0 for a normal exit)
 */
main(): Int64 {
    return 0
}