/**
 * @file[zh-cn] System
 * @brief[zh-cn] 包锚文件：three.core.system 包（系统信息）
 * @explain[zh-cn] 本包封装 sdl4cj 的系统信息能力（CPU / 电源 / 剪贴板 / 消息框 / 文件系统 / 杂项 / 日期），供 three 各模块与外部使用。根据 cjpm 的目录扫描规则，父目录必须存在至少一个 .cj 文件，否则子目录不会被扫描为源码，故保留本锚文件。
 *
 * @file[en] System
 * @brief[en] Anchor file for the three.core.system package (system information)
 * @explain[en] This package wraps sdl4cj system information capabilities (CPU / power / clipboard / message box / file system / misc / date), available for all three modules and external use. Per cjpm directory scanning rules, the parent directory must contain at least one .cj file; otherwise subdirectories will not be scanned as source, so this anchor file is retained.
 */
package three.core.system
